[commit]
    gpgsign = true
[user]
    name = mu88
    email = 4560672+mu88@users.noreply.github.com
    # Install key
    # Run "gpg --list-secret-keys"
    # Run "gpg --edit-key <<GitHub Key>>"
    # Grab the 16 char long ID "BLABLUBTRALALALA" of the entry "sec  rsa4096/BLABLUBTRALALALA"
    signingkey = 22DC58317AC29D13
[credential "https://github.com"]
    username = mu88
